/target
Cargo.lock.bak
*.pdb
*.profraw
.vscode/
.idea/
*.swp

# Locally cross-compiled test build dropped next to the repo for
# the maintainer's iteration loop. Not part of any release.
/superdupe-gui.exe
/diagnostics/
/superdupe.exe

# Per-developer Cargo overrides — see .cargo/config.toml.example
# for the river5 path-patch pattern. Tracked example, ignored real
# config.
/.cargo/config.toml
